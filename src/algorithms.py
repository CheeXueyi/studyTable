from customTypes.sessionTypes import *

def allocateStudySessions(
    subs: SubjectsToAllocate, idealSessionMinutes: int, studySessions: list[StudySession]
) -> AllocatedStudySessions:
    # to implement
    ret = AllocatedStudySessions()
    return ret